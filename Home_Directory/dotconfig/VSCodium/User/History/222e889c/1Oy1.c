#include <stdio.h>
#include <stdlib.h>

int v[10000];

int main()
{
    FILE *fin, *fout;
    int n, i, maxx;
    
    fin = fopen( "numere1.in", "r" );
    for( i = 0; i < n; i++ ){
        fscanf( fin, "%d", v[i] );
    }
    
    
}