#include <stdio.h>
#include <stdlib.h>

int main()
{
    FILE *fin, *fout;
    int numar, suma;
    char caracter;
    numar = 0;
    
    fin = fopen( "antiterra.in", "r" );

    caracter = fgetc( fin );
    while( caracter != '\n' ){
        while( caracter != ' ' ){

        }
    }
    return 0;
}